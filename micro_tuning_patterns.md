# 截图驱动微调：常见改法速查

本文件用于“第 4 步：截图驱动微调”阶段的快速对照，把用户的自然语言反馈翻译为可执行的 HTML/CSS 改动。

## 使用原则

- 优先做 **CSS 局部改动**（间距/字号/对齐/裁切/颜色），避免大改 DOM。
- 任何“看起来没排完”的问题，通常是：
  - 栅格未占满（列宽/跨列设置不合理）
  - 容器最小高度过大/过小
  - 卡片高度/底边基线不齐
- 修改后必须：本地快速验证 → 重新部署 → 返回防缓存链接。

---

## 1) 删字 / 去掉内部痕迹文案

**常见反馈**：
- “把这几个字去掉”
- “这个 HTML 要对客展示，这些内容不合适”

**执行要点**：
- 先定位文字所在区域（标题区/页脚/角标/小字注释）。
- 删除或替换为中性表达（例如“客户案例 / Customer Story / One Pager”）。
- 同步检查：是否还有“终版/校准/口径/内部”等词残留。

推荐配合：`scripts/scan_internal_copy.py`。

---

## 2) 标题要单行 / 标题不要换行

**常见反馈**：
- “标题单行”
- “标题不要折行”

**改法优先级**：
1. 桌面端强制单行：
   - `white-space: nowrap;`
   - `max-width: none;`
   - 用 `clamp()` 限制字号区间，让标题在桌面宽度下更稳。
2. 在中小屏解除单行（防止溢出）：
   - 在 `@media (max-width: ...)` 中改回 `white-space: normal;`
   - 同步下调字号。

**风险提示**：
- 单行可能导致溢出；必须同时检查：超长标题是否在 1440/1920 下可读。

---

## 3) 图片上移 / 图片更贴近上方内容 / 减少留白

**常见反馈**：
- “图片上移一点”
- “这里空太多”

**改法**：
- 如果是图片区距离上方文本太远：减少 `margin-top` 或增加负 margin（谨慎）。
- 如果是图片区本身太“横”：
  - 调整容器比例 `aspect-ratio`
  - 使用 `object-position` 把主体上移（例如 `object-position: 40% 16%`）
  - 通过 `transform: scale(...)` 轻微放大以避免露底。

---

## 4) 布局收紧 / 信息密度更像 PPT

**常见反馈**：
- “布局收紧一点”
- “整体更像一页纸”

**改法**：
- 降低全局 gap（例如 `--grid-gap`）
- 减小卡片 padding（但保持可读性）
- 减少段落上下 margin
- 统一列表行高与字号（正文 12-14px 常见）

---

## 5) 底部空白 / 底部看起来没排完

**常见反馈**：
- “最底部空空的”
- “底部未排完”

**根因排查顺序**：
1. 栅格占位：底部模块是否只占了左侧列？右侧列是否空置？
2. 模块高度：是否存在 `min-height` 过高导致视觉断层？
3. 卡片数量：底部是否缺少“收束型内容”（总结、事实信息、关键指标补充）？

**通用解决套路**：
- 把底部从“孤立小卡片”改为 **横跨整行的信息带**。
- 改造成 **左侧主叙事 + 右侧信息栈（并列卡片/列表）**。
- 必要时增加“总结收束模块”，让页面最后一屏像“正式结尾”。

---

## 6) 图片替换 / 提亮 / 压暗 / 裁切（对客化处理）

**常见反馈**：
- “换成这张图”
- “图太亮/太白/太抢了”

**允许的处理方式（不改变事实内容）**：
- 使用 CSS：`filter: brightness()/contrast()/saturate()`
- 使用遮罩层叠加：在容器上加 `::after`，叠一层渐变以压暗高光
- 使用裁切：通过 `object-fit: cover` + `object-position` 完成“视窗裁切”

**必须遵守**：
- 图片必须来自可追溯来源（URL 或用户上传）。
- 不能“补出不存在的人/物/Logo”。

---

## 7) 三列内容底边不齐 / 卡片高度不统一

**改法**：
- 使用 CSS Grid/Flex 让卡片 `align-items: stretch`。
- 为卡片设置统一的 `min-height`（谨慎，避免移动端过长）。
- 将卡片内部结构改为 `display: flex; flex-direction: column;` 并把“底部信息”放到 `margin-top: auto;`，实现底部对齐。

---

## 8) 缓存导致用户看到旧版本

**必做动作**：
- 每轮都返回“新部署链接”。
- 同时返回防缓存打开方式：`?v=YYYYMMDD-HHMM`。
- 给强刷快捷键：Windows `Ctrl+Shift+R` / Mac `Cmd+Shift+R`。
