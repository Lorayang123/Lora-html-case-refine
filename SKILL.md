---
name: html-case-refine-redeploy
description: 将任意客户案例 HTML 单页/目录按参考模板重制为可对客展示版本，支持模板视觉语言对齐、清理内部制作痕迹、多轮截图反馈驱动的局部微调、真实可追溯素材抓取接入，并在每轮修改后重新部署返回最新可访问链接；适用于售前/客户成功的 SaaS 客户案例一页纸、对外演示页、投屏截图页精修与交付。
---

# 客户案例 HTML 对客展示精修 + 多轮重部署（可复用工作流）

## 适用范围（何时触发）

- 需要把“已有客户案例 HTML 页面”改造成**看起来属于某一套统一模板体系**（如飞书客户案例模板、客户自有品牌模板、深色一页纸模板等）。
- 需要将页面升级到**可直接对客展示**（投屏/截图进方案/发客户预览链接），要求专业、统一、无内部痕迹。
- 用户以“截图圈选 + 文字指令”的方式提出多轮微调（删字、标题单行、图片上移、布局收紧、底部补齐、图片替换/提亮/裁切等），且每轮都要**重新部署**给到新链接。

### 触发词 / 典型说法（任意命中即可）
- “按这个 PPT/模板把我的客户案例 HTML 重制一版”
- “这个 HTML 要对客展示，帮我清掉内部痕迹并精修”
- “我会多轮截图反馈，每轮改完都重新部署给我链接”
- “从这个网页链接里抓一张真实图片放到页面里”

## 输入（把具体文件名抽象为参数）

### 必选输入

1. **原始 HTML 页面或目录**（二选一）
   - 单文件：一个可直接打开的 `*.html` 文件（内联 CSS/JS 或相对资源引用均可）。
   - 目录：包含入口 `index.html`（或可指定入口文件）以及 `assets/` 等静态资源。

2. **参考模板（至少一种）**
   - PPTX / PDF / 可读文档（docx、markdown 等）
   - 图片截图（模板页截图、竞品参考页截图、客户品牌手册截图）
   - Wiki / 在线文档链接（若链接无法读取，必须按“失败处理”走阻塞说明）

### 可选输入（用于提高完成度）

3. **多轮反馈材料**
   - 用户截图（PNG/JPG），最好带圈选或明确指向区域
   - 用户文字说明（例如“这几个字去掉”“标题要单行”“图片上移一点”）

4. **真实素材接入来源**
   - 网页链接（文章页、新闻稿、公众号等）
   - 直接图片 URL
   - 用户上传的图片文件

## 输出（交付规范）

每轮迭代都要产出下面 3 类输出（缺一不可）：

1. **本地可复用的页面目录**
   - 推荐命名：`deploy_<case>_vN/`（N 递增）
   - 目录内至少包含：`index.html` + `assets/`（如有）

2. **最新部署链接**
   - 返回一个可访问的线上链接（例如 `https://example.com/your-deploy-url`）
   - 同时给一个**防缓存**打开方式（例如在 URL 后追加 `?v=YYYYMMDD-HHMM`）

3. **变更说明（适合售前协作的写法）**
   - 用“本轮改了什么 / 为什么这样更对客 / 下一轮需要你确认什么”三段式说明
   - 严禁出现内部制作痕迹（如“终版校准”“口径建议”“模板还原度不足说明”这类内容）

## 核心约束（必须遵守）

- **不能伪造素材**：外部图片/文案必须可追溯到原始 URL 或用户上传文件；不得编造、补图凑数。
- **对客版必须清理内部痕迹**：任何“制作备注/校准说明/内部口径/占位提示/TODO”都不得出现在对客页面。
- **模板优先级**：用户提供的新模板（最新一份）优先级最高；如与旧模板冲突，以新模板为准。
- **流水线顺序不可跳步**：先“模板对齐 + 对客清理”，再进入“截图驱动微调循环”，最后做“最终交付检查”。
- **外部链接/素材不可读时必须阻塞说明**：要给出阻塞原因、已尝试手段、替代方案（让用户补充可用文件/截图/直链）。

---

## 标准工作流（按顺序执行）

### 第 0 步：建立“可迭代”的交付结构

- 保留原始输入不动（只读基线）。
- 创建一个工作目录作为 v1/v2… 的起点：
  - 若输入是单 HTML：创建 `deploy_<case>_v1/` 并把入口命名为 `index.html`。
  - 若输入是目录：复制为 `deploy_<case>_v1/`。
- 每轮修改只在最新版本目录上做（或复制出 vN+1 再改），避免回滚困难。

> 可选脚本：用 `scripts/bump_version_dir.py` 复制出新版本目录（见脚本说明）。

### 第 1 步：获取并解析参考模板（形成“风格简报”）

目标：把“模板长什么样”拆成可落地的设计约束（色板/字体层级/栅格/卡片语言/品牌区规则）。

- **模板是 PPTX**：
  - 优先提取缩略图/导出图片，直接以视觉结果为准（比只读文字更可靠）。
  - 可选做法：解压 PPTX 并读取 `docProps/thumbnail.jpeg`（有时足够代表模板视觉）。
- **模板是图片截图**：直接以截图为准。
- **模板是 Wiki/在线文档链接**：
  - 尝试用可用的文档下载能力读取。
  - 若链接不可读取（例如权限/环境限制/站点不允许抓取），必须立刻输出“阻塞说明”，并请求用户提供：
    - PPTX/PDF 文件链接，或
    - 可下载的 docx 直链，或
    - 关键页截图（至少包含封面区+正文卡片区）。

产出一个“风格简报”（写在当轮回复里即可，不要求落文件），至少包含：
- 背景：浅色/深色、渐变方向、噪点/纹理
- 文字层级：主标题/副标题/正文/注释的字号与粗细倾向
- 栅格：常见列数（1/2/3/4）、边距与卡片间距节奏
- 卡片语言：圆角、描边、阴影、强调色条、标签 chip 的形态
- 品牌区：Logo 放置位置、大小克制程度、是否有品牌条

### 第 2 步：按模板做“风格对齐重制”（先大后小）

先把“模板归属感”做出来，再考虑细节微调。

- 统一设计 Token：背景色、主色/辅色、圆角、阴影、描边透明度、间距节奏。
- 重做页面关键结构（典型是一页纸结构）：
  - 顶部标题区（左标题、右品牌区、下方标签/辅助信息）
  - 核心数字卡/指标卡（通常是模板的强识别点）
  - 主叙事卡（背景/方案/价值/洞察/行动等）
  - 客户原声区（引用 + 署名 + 可选人物图）
  - 底部收束区（避免“看起来没排完”）
- 不追求“字字像 PPT”，追求：一眼看过去属于同一套模板体系。

### 第 3 步：对客文案清理（把内部制作痕迹彻底擦掉）

- 全局扫描并移除这些类型文字：
  - “终版校准 / 模板体系 / 口径建议 / 内部备注 / TODO / 占位 / 草稿 / 给内部看”
  - 任何自我解释式话术（例如“当前环境无法…”这类）
- 替换为中性对外表达：
  - 标题区的辅助文案可统一为“客户案例 / Customer Story / One Pager”等
  - 页脚若无必要，直接删除或保留极简版权信息

> 可选脚本：用 `scripts/scan_internal_copy.py` 扫描页面中可能的内部痕迹词。

### 第 4 步：进入“截图驱动微调 + 实时重部署”循环（多轮）

这是本 Skill 的核心闭环：每轮都要**小步快改、可验证、可回滚**。

**每一轮都按以下顺序执行：**

1. **读取反馈**
   - 如果用户给截图：优先以截图为准，圈选区域最高优先级。
   - 如果用户只给文字：先定位对应 DOM/样式，再动手。

2. **将反馈翻译成“可执行改动点”**
   - 改的是文案？样式？布局结构？还是素材？
   - 改动范围是局部 CSS（优先）还是需要重排 DOM（慎用）？

3. **实施修改（最小改动原则）**
   - 只改用户指出的区域，避免引入连锁变化。
   - 常见微调模式速查见：`references/micro_tuning_patterns.md`

4. **本地快速验证**
   - 目标：不溢出、不乱码、不破版、对比度可读、主要标题识别效率高。

5. **重新部署并返回新链接**
   - 每轮必须部署，不能只给“本地文件改好了”。
   - 必须提供防缓存参数（例如 `?v=YYYYMMDD-HHMM`），并给出强刷指引（Ctrl/Cmd+Shift+R）。

6. **记录版本与变更**
   - 至少记录：vN → vN+1 改了什么、部署链接、用户是否确认。

### 第 5 步：真实素材接入（必须可追溯）

当需要“从网页链接获取真实图片并接入页面”时：

1. **先证明素材可追溯**
   - 记录原始页面 URL。
   - 记录最终使用的图片 URL（若是从页面中解析出来）。

2. **获取素材**
   - 优先走可复现的页面抽取（例如公众号文章优先用“网页抽取”而不是手抄）。
   - 若站点强制登录/验证码/反爬导致拿不到图片：
     - 立即中止该分支（不继续“凭想象补图”），输出阻塞说明
     - 给替代方案：让用户上传图片文件、或提供可下载直链、或提供截图

3. **落盘并接入 HTML**
   - 把图片保存到当前版本目录的 `assets/` 下，使用语义化文件名（例如 `assets/founder_portrait.jpg`）。
   - HTML 引用尽量用相对路径，保证部署稳定。

4. **做“对客化的轻处理”（允许裁切/压暗/提亮，但不得伪造内容）**
   - 优先用 CSS 做无损处理：`object-fit`、`object-position`、`filter`、遮罩渐变叠层。
   - 仅在确实需要时做实际裁切（且保留原图或记录来源）。

> 可选脚本：用 `scripts/download_asset.py` 下载图片并写入可追溯记录（见脚本说明）。

### 第 6 步：最终交付检查（对客成品感）

- 统一性：颜色、字体层级、圆角、阴影、卡片间距一致。
- 专业性：无内部备注、无“模板没排完”观感、底部有明确收口。
- 可读性：对比度足够，列表与注释不发灰发虚。
- 叙事完整：背景-方案-价值-客户原声-收束链路闭环。

---

## 失败处理与阻塞报告模板（必须输出给用户）

当任一关键输入不可用时，停止“继续设计生成”，改输出：

1. **阻塞点**：具体是什么读不到/拿不到（模板链接不可下载、图片防盗链、权限不足等）。
2. **已尝试手段**：例如尝试下载/抽取/解析。
3. **影响范围**：会影响哪些对齐点（像素级栅格/字号无法 1:1 等）。
4. **替代方案**（给用户可执行的下一步）：
   - 提供 PPTX/PDF/docx 文件或直链
   - 提供关键页截图
   - 直接上传图片文件

---

## 附：可复用脚本

### scripts/bump_version_dir.py
- 用途：从 `deploy_<case>_vN/` 复制出 `deploy_<case>_vNplus1/`，保持每轮改动可回滚。
- 运行示例：

```bash
python3 scripts/bump_version_dir.py --src deploy_<case>_v2 --dst deploy_<case>_v3
```

### scripts/scan_internal_copy.py
- 用途：扫描 HTML 中可能的内部痕迹关键词，辅助对客清理。
- 运行示例：

```bash
python3 scripts/scan_internal_copy.py --file deploy_<case>_v3/index.html
```

### scripts/download_asset.py
- 用途：下载外部图片到 `assets/` 并记录来源 URL（用于“素材可追溯”）。
- 运行示例：

```bash
python3 scripts/download_asset.py \
  --url "https://example.com/a.jpg" \
  --out "deploy_<case>_v3/assets/founder.jpg" \
  --referer "https://example.com/article" \
  --manifest "deploy_<case>_v3/assets/asset_manifest.jsonl"
```

> 以上脚本属于“护栏工具”，不是强制要求；无法运行时可按流程手动完成，但必须保留可追溯证据。
